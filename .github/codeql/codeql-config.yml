# Область анализа — только сервер.
#
# Без этого CodeQL разобрал бы заодно Android-приложение: это тоже Kotlin, и в область
# `java-kotlin` оно попадает само. Расширение на клиент — отдельная работа, а пока смотреть
# находки по нему некому.
name: MedAppServer

paths:
  - src/MedAppServer/src
